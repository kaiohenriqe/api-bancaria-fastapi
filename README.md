# 🏦 API Bancária com FastAPI

Projeto simples de uma API bancária usando **FastAPI**.  
Aqui dá pra fazer depósitos, saques e ver o extrato da conta.  
Os endpoints são protegidos com **JWT** (login obrigatório).

---

## 🚀 Como rodar

1. Clone o repositório:
   ```bash
   git clone https://github.com/kaiohenriqe/api-bancaria-fastapi.git
   ```
   cd api-bancaria-fastapi
   ``
   Crie e ative o ambiente virtual:
    ```bash
    python -m venv .venv
    .venv\Scripts\activate   # Windows
    source .venv/bin/activate # Linux/Mac
     ```
